server:
  port: 8788
  
spring:
  application:
    name: warehouseenter
  zipkin:
    base-url: http://localhost:9411

feign:
  hystrix:
    enabled: true
hystrix:
  command:
    default:
      execution:
        isolation:
          strategy: SEMAPHORE

security:
  oauth2:
    resource:
      jwt:
        key-value: test-secret
        
testkey: aaaaaaaaaaaa
















